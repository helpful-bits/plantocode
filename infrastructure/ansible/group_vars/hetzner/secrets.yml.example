# Hetzner-specific secrets
# Copy this to secrets.yml and fill in your actual values, then encrypt with:
# ansible-vault encrypt group_vars/hetzner/secrets.yml --vault-password-file .vault_pass

# Stripe Webhook Secret for Hetzner deployment
# This should be the webhook signing secret from your Stripe dashboard
# for the webhook endpoint: https://api-eu.plantocode.com/webhooks/stripe
stripe_webhook_secret_hetzner: "whsec_YOUR_HETZNER_STRIPE_WEBHOOK_SECRET"