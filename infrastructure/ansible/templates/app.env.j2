# {{ ansible_managed }}
# Application Environment Configuration
# Generated on {{ ansible_date_time.iso8601 }}
# This file is completely replaced on each deployment
# Note: SERVER_PORT is excluded - controlled by systemd per service (blue/green)

{% for key, value in app_env_vars.items() | sort %}
{% if key != "SERVER_PORT" -%}
{{ key }}={{ value }}
{% endif -%}
{% endfor %}