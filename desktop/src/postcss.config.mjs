/** @type {import('postcss-load-config').Config} */
const config = { // Keep config object
  plugins: {
    tailwindcss: {}, // Keep tailwindcss plugin
    autoprefixer: {},
  }, // Keep plugins
};

export default config;
