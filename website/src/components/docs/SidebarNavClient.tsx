'use client';

import type { DocGroup } from '@/docs/docs-manifest';

interface SidebarNavClientProps {
  data: DocGroup[];
  currentPath: string;
}

export function SidebarNavClient(_props: SidebarNavClientProps) {
  return null;
}