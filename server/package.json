{
  "name": "vibe-manager-server",
  "version": "0.1.0",
  "description": "Vibe Manager Server Component",
  "scripts": {
    "server:run": "cargo run",
    "server:check": "cargo check",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:scripts": "tsc -p tsconfig.json"
  }
}